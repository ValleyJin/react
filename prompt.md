당신은 React 입문 강의용 예제 프로젝트를 만드는 시니어 프론트엔드 엔지니어이자 교육용 콘텐츠 설계자다.
나는 초보자 대상의 React 강의를 만들고 있다.  
아래 5강 흐름에 맞춰, 각 강의별로 폴더를 만들고, 그 안에 독립 실행 가능한 예제 프로젝트와 설명 자료(md파일)를 만들어라.
## 강의 구조
1. 리액트의 사고방식
2. 컴포넌트, Props, State
3. 리렌더링과 데이터 흐름
4. Hooks와 로직 분리
5. 성능 최적화와 설계 전략
## 전체 목표
이 강의의 목표는 React 문법 나열이 아니라 다음 3가지를 익히게 하는 것이다.
1. UI를 상태의 결과로 보는 사고방식
2. 데이터 흐름과 렌더링의 관계 이해
3. 로직을 분리하고 재사용하는 실무 감각
## 각 강의별 핵심 방향
### 1강. 리액트의 사고방식
- 선언형 프로그래밍과 컴포넌트 기반 UI를 이해시키는 것이 목표다.
- 명령형 방식(JS / jQuery식 DOM 조작)과 React 방식을 비교하라.
- "UI = f(state)" 관점을 설명할 수 있는 예제를 만들어라.
- React에서 상태를 바꾸면 UI가 다시 계산된다는 흐름이 드러나야 한다.
### 2강. 컴포넌트, Props, State
- 함수형 컴포넌트, JSX, props, state, 이벤트 처리를 가르치는 것이 목표다.
- props는 외부에서 받는 값, state는 내부에서 바꾸는 값이라는 차이가 드러나야 한다.
- useState를 사용하는 간단한 입력/토글/카운터 예제를 만들어라.
### 3강. 리렌더링과 데이터 흐름
- 단방향 데이터 흐름, 부모-자식 관계, state lifting을 이해시키는 것이 목표다.
- 상태 위치가 구조를 결정한다는 점이 드러나야 한다.
- 조건부 렌더링, 리스트 렌더링, key 개념도 포함하라.
### 4강. Hooks와 로직 분리
- useEffect와 Custom Hook을 통해 UI와 상태ful 로직을 분리하는 것이 목표다.
- 렌더링 로직과 부수효과의 차이를 명확히 드러내라.
- 잘못된 useEffect 사용 예시와 올바른 사용 예시를 비교하라.
- 반복 로직을 Custom Hook으로 추출하는 예제를 만들어라.
### 5강. 성능 최적화와 설계 전략
- memo, useMemo, useCallback을 소개하되, 무조건 쓰는 것이 아니라 필요할 때 쓰는 것임을 보여줘라.
- 구조 문제와 최적화 문제를 구분하는 감각을 길러야 한다.
- 상태 위치와 컴포넌트 구조를 먼저 점검해야 한다는 메시지를 포함하라.
## 반드시 지켜야 할 출력 형식
각 강의마다 아래 항목을 같은 순서로 작성하라.
1. 강의 제목
2. 학습 목표
3. 예제 프로젝트 개요
4. 핵심 개념 설명
5. 파일 구조
6. 전체 코드
7. 코드 설명
8. 학생이 실수하기 쉬운 포인트
9. 실습 과제 2~3개
10. 다음 강의로 이어지는 연결 설명
## 코드 생성 규칙
- React 초보자가 이해할 수 있도록 파일 수를 최소화하라.
- 기본적으로 Vite + React 기준으로 작성하라.
- 가능하면 JavaScript 버전으로 먼저 작성하라. TypeScript는 필요 시 추가로 제안하라.
- 각 예제는 독립 실행 가능해야 한다.
- 코드에는 주석을 충분히 달아라.
- 설명은 반드시 한국어로 작성하라.
- 각 강의의 예제는 앞 강의보다 난이도가 조금씩 올라가야 한다.
- 너무 많은 추상화나 과한 폴더 분리를 하지 마라.
- 성능 최적화는 실제로 왜 필요한지 분석한 후에만 적용하라.
- 학생이 구조를 이해할 수 있도록 "왜 이렇게 나눴는가"를 설명하라.
## 추가 요구사항
- 마지막에는 5강 전체를 관통하는 요약표를 만들어라.
- 요약표에는 각 강의의 핵심 개념, 대표 예제, 자주 하는 실수, 실무 연결 포인트를 포함하라.
- 출력은 마크다운으로 정리하라.